TODO
====
- It is possible not to have to call Build on classifiers and have the evaluators
  call this if it has not been previously called.  To consider.
- Change classifier index in Runtime constructor to be a string with the property name.